import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    bgBlack: "#141414",
    modalBgBlack: "#181818",
    fontWhite: "#e5e5e5",
    vanflixRed: "#d81f26",
  },
});

export default theme;
