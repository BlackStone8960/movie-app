import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    bgBlack: "#141414",
    modalBgBlack: "#181818",
    clickedGray: "#333",
    fontWhite: "#e5e5e5",
  },
});

export default theme;
